name: CI/CD - Testing

on:
  pull_request:
    branches:
      - testing
  push:
    branches:
      - testing
  workflow_dispatch:

concurrency:
  group: retail-store-testing-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read
  security-events: write

jobs:
  code_quality:
    name: Quality
    uses: ./.github/workflows/reusable-code-quality.yml
    # nosemgrep
    secrets: inherit

  security_analysis:
    name: Security
    uses: ./.github/workflows/reusable-security-analysis.yml
    # nosemgrep
    secrets: inherit

  build_images:
    name: Build
    needs:
      - code_quality
      - security_analysis
    uses: ./.github/workflows/reusable-build-images.yml
    with:
      image-tag: ${{ github.sha }}

  automated_tests:
    name: Tests
    needs: build_images
    uses: ./.github/workflows/reusable-automated-tests.yml

  registry_bootstrap:
    name: Registry Bootstrap
    if: github.event_name != 'pull_request'
    needs:
      - automated_tests
    uses: ./.github/workflows/reusable-registry-bootstrap.yml
    with:
      environment: test
      aws-region: us-east-1
    # nosemgrep
    secrets: inherit

  gate_dev_to_test:
    name: Quality gate Dev -> Test
    runs-on: ubuntu-latest
    needs:
      - registry_bootstrap
    if: github.event_name != 'pull_request'
    steps:
      - name: Verify Dev promotion gate
        run: |
          {
            echo "## Quality gate Dev -> Test"
            echo ""
            echo "| Control | Estado |"
            echo "| --- | :---: |"
            echo "| SonarCloud quality gate | Aprobado |"
            echo "| Analisis de seguridad | Aprobado |"
            echo "| Pruebas automatizadas | Aprobado |"
            echo "| Imagenes publicadas en ECR Test | Aprobado |"
            echo ""
            echo "Commit promovido: \`${GITHUB_SHA}\`"
          } >> "$GITHUB_STEP_SUMMARY"

          echo "Quality gate Dev -> Test aprobado."

  publish_images:
    name: Publish
    if: github.event_name != 'pull_request'
    needs: gate_dev_to_test
    uses: ./.github/workflows/reusable-publish-images.yml
    with:
      aws-region: us-east-1
      ecr-repository-prefix: devops-retail-store-test
      image-tag: ${{ github.sha }}
    # nosemgrep
    secrets: inherit

  deploy_test:
    name: Deploy Test
    needs: publish_images
    if: github.event_name != 'pull_request'
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: test
      github-environment: Test
      aws-region: us-east-1
      image-tag: ${{ github.sha }}
    # nosemgrep
    secrets: inherit
