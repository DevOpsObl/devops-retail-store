{
  "name": "retailstore-deployment-validator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "check": "node --check src/health-check.mjs && node --check src/handler.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.1075.0",
    "@aws-sdk/client-sns": "^3.1075.0"
  }
}
